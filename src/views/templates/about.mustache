{{> header}}

<div class="markguide-landing">
    <div class="markguide-landing__content">
        <div class="markguide-landing__aside">
            {{> toc}}
        </div>
        <main class="markguide-landing__main">
            <section>
                <!-- this is hacky, but we have sections in markdown, so we need to open and close it on wrapper -->
                {{{content.documentation}}}
            </section>
        </main>
    </div>
</div>

{{> footer}}
